package hamming

Error :: enum {} // Please inspect the tests to see which error conditions to enumerate

distance :: proc(strand1, strand2: string) -> (int, Error) {
	#panic("Please implement the `distance` procedure.")
}
