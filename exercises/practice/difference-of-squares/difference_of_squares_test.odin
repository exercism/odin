package difference_of_squares

import "core:testing"

@(test)
test_square_the_sum_of_the_numbers_up_to_the_given_number :: proc(t: ^testing.T) {
	expected := null
    input := `null`
    result := null(input)

	testing.expect_value(t, result, expected)
}

@(test)
test_sum_the_squares_of_the_numbers_up_to_the_given_number :: proc(t: ^testing.T) {
	expected := null
    input := `null`
    result := null(input)

	testing.expect_value(t, result, expected)
}

@(test)
test_subtract_sum_of_squares_from_square_of_sums :: proc(t: ^testing.T) {
	expected := null
    input := `null`
    result := null(input)

	testing.expect_value(t, result, expected)
}

